<?php
/**
 * 数据库表生成类
 *
 * 调用方法
 * php cp model
 * 指定配置项
 * php cp model:main
 *
 * 基本配置
 * db 指定数据库名称, 与数据库配置文件中的一致
 * type 指定生成class或trait
 * namespace 指定命名空间前缀
 * models 待生成的配置数组[生成对象名称 => 数据表配置]
 * 数据表配置默认传表名，当需要分表时传数组
 * - field  分表规则字段
 * - prefix 分表前缀，类数据结构从表prefix0获取
 * - method 分表方法，mod或hash
 * - number 分成多少张数量
 */
return [
    'main' => [
        'db' => 'mysql:db',
        //class或trait
        'type' => 'class',
        //生成类的命名空间前缀
        'namespace' => 'structural',
        //类名 => 表名数组
        'models' => [

        ]
    ]
];

